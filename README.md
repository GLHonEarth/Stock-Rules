# 📈 股票智能技术分析与策略决策系统

根据 `request.md`（PRD）实现。系统通过**免费公开数据接口**获取单只 A 股的历史与实时行情，
内置**四大策略引擎**（传统技术面/马丁/反马丁/定投），自动计算买卖点信号，并在
Streamlit 可视化看板中展示，辅助投资决策。

> ⚠️ **免责声明**：本项目为技术分析演示工具，所有信号与回测结果**不构成任何投资建议**。
> 股市有风险，投资需谨慎。马丁策略等高风险策略仅供学习研究。

---

## 一、功能总览（对应 PRD）

| PRD 章节 | 功能 | 实现位置 |
|---|---|---|
| 2.1 数据获取 | 东方财富/新浪/百度/巨潮 免费接口，多源自动降级 | [core/data_fetcher.py](core/data_fetcher.py) |
| 2.3 反爬风控 | 请求间隔 0.6s、失败重试（指数退避）、UA 轮换、磁盘缓存 | [core/config.py](core/config.py) |
| 3.1 传统分析 | MA 金叉死叉 / MACD / KDJ / 基本面（PE百分位+业绩增长） | [core/strategies.py](core/strategies.py) |
| 3.2 马丁策略 | 越跌越买、×2 倍加仓、目标微利 2% 平仓、**最大 5 次加仓+止损线强制风控** | 同上 |
| 3.3 反马丁策略 | 赢冲输缩：均线多头建仓、突破新高盈利加仓、跌破 MA5/止损离场 | 同上 |
| 3.4 定投策略 | 每周四定投、PE<50% 加倍/MA20 低吸加码、目标 20% 分批止盈 | 同上 |
| 4. UI 看板 | 搜索框、周期切换、策略开关、K线+买卖点、成交量/MACD/KDJ/RSI 副图、信号看板、持仓模拟计算器、基本面卡片、五档盘口 | [app.py](app.py) |

## 二、环境要求

- Windows / macOS / Linux 均可
- Python 3.9 及以上（推荐 Anaconda，本项目在 Python 3.10 上开发验证）
- 需要联网（数据来自东方财富、新浪财经、百度股市通、巨潮资讯等公开接口）

## 三、安装与启动（三步）

### 第 1 步：进入项目目录

```bash
cd d:\Code\股票纪律
```

### 第 2 步：安装依赖

```bash
pip install -r requirements.txt
```

> 国内网络若安装缓慢，可用清华镜像加速：
> `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 第 3 步：启动系统

```bash
streamlit run app.py
```

启动成功后，终端会显示本地访问地址，浏览器会自动打开
**http://localhost:8501**（若未自动打开，手动复制该地址到浏览器）。

---

### 🪟 Windows 小白方式（双击即可）

双击项目目录下的 **`启动系统.bat`**，脚本会自动检查/安装依赖并启动系统，
无需手动敲命令。

---

## 四、界面使用说明

1. **左侧控制区**
   - 输入股票代码（如 `600519`、`000001`）或名称（如 `贵州茅台`），回车确认
   - 切换周期：日K / 周K / 月K / 分时
   - 勾选需要启用的策略（默认全部启用）
   - 设置模拟初始资金（用于持仓模拟计算器）
   - 调整 K 线显示数量；网络异常时可点击「强制刷新数据」

2. **顶部实时行情卡**：最新价、涨跌幅、今开/昨收、最高/最低、成交量/额、更新时间

3. **当前策略信号看板**：四大策略的状态卡片（如“传统指标：看多”、“马丁策略：等待加仓”），
   带风险提示折叠区

4. **核心图表区**
   - 主图：K 线 + MA5/10/20/60 + 布林带；**🟢▲ 绿箭头=买点，🔴▼ 红箭头=卖点**，悬停查看原因
   - 鼠标拖拽平移、滚轮缩放
   - 副图：成交量（红涨绿跌）、MACD、KDJ、RSI

5. **持仓模拟计算器**：按所选初始资金回测各策略，展示当前理论持仓数、成本价、市值、盈亏比例、
   仓位占比，以及历史盈亏率曲线

6. **基本面卡片**：PE(TTM)、PE 一年百分位、PB、净利润增长率、所属行业、上市日期

7. **五档盘口**：实时买卖五档报价

## 五、常见问题（FAQ）

| 问题 | 解决办法 |
|---|---|
| 页面提示“数据加载失败/网络连接” | 数据源为免费公开接口，偶发限流。等 1~2 分钟点击左侧「强制刷新数据」，或直接刷新浏览器页面。系统内置 3 次重试 + 多数据源自动降级 |
| 首次加载较慢（几秒~几十秒） | 正常。首次需抓取历史行情、估值、财务等数据，之后数据会缓存到本地 `cache/` 目录，刷新秒开 |
| 输入股票名称搜不到 | 首次名称搜索需下载全市场股票列表（约 30~40 秒），之后缓存 12 小时。也可以直接输入 6 位代码 |
| 分时模式下没有策略信号 | 正常。分时仅展示当日走势，策略信号基于日/周/月 K 线计算 |
| 不同周期/不同显示数量下策略结果不同 | 策略在所选 K 线周期的**完整历史**上回测（指标边界值连续），图表仅影响展示多少根 K 线 |
| 想调整策略参数（加仓倍数、止盈目标等） | 编辑 [core/config.py](core/config.py) 中的 `STRATEGY_PARAMS`，保存后刷新页面即可 |
| 端口 8501 被占用 | 终端执行 `streamlit run app.py --server.port 8502` |

## 六、项目结构

```
股票纪律/
├── app.py                  # Streamlit 主界面（入口）
├── core/
│   ├── config.py           # 全局配置：请求限速/重试/缓存/策略参数
│   ├── data_fetcher.py     # 数据获取：多源降级 + 重试 + 磁盘缓存
│   ├── indicators.py       # 技术指标：MA / MACD / KDJ / RSI / BOLL
│   ├── strategies.py       # 四大策略引擎（传统/马丁/反马丁/定投）
│   └── position.py         # 持仓模拟计算器
├── test_engine.py          # 引擎自检脚本（python test_engine.py）
├── requirements.txt        # 依赖清单
├── 启动系统.bat            # Windows 一键启动
├── cache/                  # 数据缓存目录（自动生成）
└── request.md              # 原始需求文档（PRD）
```

## 七、技术说明

- **数据源**（全部免费、无需 API Key）：东方财富（历史K线）、新浪财经（实时行情/五档/分时）、
  百度股市通（PE/PB 估值及百分位）、巨潮资讯（行业/上市日期）、新浪财务（净利润增长率）
- **估值百分位**：PE 与自身近一年历史比较，避免依赖不可靠的行业平均数据；
  <50% 视为低估、>80% 视为高估（智能定投与基本面策略使用）
- **马丁策略风控（PRD 3.2）**：强制 `最大加仓次数=5`、`总资金止损线=-10%`，触发后停止交易并在界面红色警示
- **缓存策略**：实时行情 20 秒、日K 20 分钟、周/月K 6 小时、估值 12 小时、公司资料 7 天；
  缓存目录 `cache/` 可随时手动清空
